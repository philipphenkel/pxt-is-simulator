namespace control {

  /**
  * Returns true if the code is currenly running in the simulator, false otherwise.
  */
  //% shim=control::isSimulator
  export function isSimulator(): boolean  { return true }
}